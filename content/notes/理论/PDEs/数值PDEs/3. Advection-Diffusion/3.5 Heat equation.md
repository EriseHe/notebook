
### Heat equation

$$
\left(  \frac{ \partial u }{ \partial t } , \sigma \right) + a (u,v) = F (v)
$$
consider 
$$
\frac{ \partial u }{ \partial t } - \mu \nabla  u+  \rho \nabla u + \sigma u = f
$$
where $x \in \Omega$ and $t > 0$. 

THeorem 

If 
$$u(x,0) = u_{0} (x) \in L^{2}(a), f\in 
$$

[missing]



$$
w = e^{\lambda t}u
$$

$$
\begin{align}
e^{\lambda t}\frac{ \partial u }{ \partial t }  = \frac{ \partial w}{ \partial t } - \lambda e^{-\lambda t} \\
e^{\lambda t} \left( \frac{ \partial u }{ \partial t } , v  \right) + (e^{\lambda t} u, v)= e^{\lambda t }F (v)
\end{align}
$$

when you multiply the expential, we have the in time
$$
\begin{align}
  & \left( \frac{ \partial w }{ \partial t }  +\lambda u, v\right) + a(w, v )= G(v) \\
 & \left( \frac{ \partial w }{ \partial t } ,v\right) 
 + \lambda(u,v)+ \underbrace{ a(w, v)  }_{ W.C. }= G(a)\end{align}
$$
where 
$$
\tilde{a}(w,v) \geq v \|w \| \text{[missing]}
$$
Some priority bounds. Two tricks to find bounds, 

consider this problem
$$
\left( \frac{ \partial u}{ \partial t } , v \right) + a (u,v) = F(v)
$$
lets take $v= u$, so we have
$$
\begin{align}
\int _{\Omega}\frac{ \partial u }{ \partial t }  u\longrightarrow \frac{1}{2} \int _{\Omega}\frac{ \partial(u^{2}) }{ \partial t }  \underbrace{ = }_{ \Omega \text{} } \frac{1}{2} \frac{d}{dt}  \|u\|^{2}_{L^{2}} \\
\leq \|f\|_{L^{2}} \|u\|_{L^{2}}
\end{align}

$$
and if we integrate everything from $0$ to $\tau$. then we have yang inequality:

$$
a b \leq \Sigma a^{2} + \frac{r}{4\epsilon}b^{2} O \leq \left( \sqrt{ \epsilon}a + \frac{1}{2 \sqrt{ \epsilon }}b \right)^{2} 
$$

[missing]

$$
\frac{1}{2} \frac{d}{dt} \|u\|^{2} + \alpha \|u\|^{2}_{H^{2}} + d
$$
then integrate:

$$
\begin{align}
\int^{\tau}_{0}\frac{d}{dt} \|u\|^{2} +2 \alpha \int^{\tau}_{0}\|u\|^{2}_{H^{2}} + d \int^{\tau}_{0} \|u\|^{2}_{L^{2}}\leq \frac{1}{\lambda} \int^{\tau}_{0} \|f\|^{2}_{L^{2}} \\ 
\|u\|^{2}_{L^{2}} (T) + 2\alpha \int^{\tau}_{0} \|u\|^{2}_{H^{1}} + \lambda\int^{\tau}_{0} \|u\|^{2}_{L^{2}} \leq \frac{1}{\lambda} \int^{\tau}_{0} \|f\|^{2}_{L^{2}} + [\text{missing}]
\end{align}
$$
[missing]

### Granwall Lemma

A way to control function when we can control end of the time of this function, and history. For a function, say
$$
g(r)\leq \alpha + \int_{0}^t\underbrace{ \beta }_{ \geq {0} } g (\tau) d\tau
$$
We can find some control of g so
$$
g(t ) \Longrightarrow \alpha + \int \alpha \beta e ^{\int^{t}_{\tau}\beta( [\text{missing}])d [\text{missing}]} d\tau
$$


$$
\|u\| ^{2} _{L^{2}} (\tau )\leq \int \underbrace{ f  }_{ \text{missing} }\|u\|_{L^{2}}+\underbrace{ \|u\|^{2} }_{ \alpha }
$$



### Another problem

$$
\frac{du}{dr} = \mu^{-1} A u + u ^{-1}
$$
where $u=[u(x_{i})]$. To solve 
$$
\frac{du}{dt}= A u + b
$$
We introduce an another concepts: Absolute stability. Then if we know
$$
\frac{du}{dt}= \lambda u 
$$
if this stability thing 
$$
u e ^{t\lambda}
$$ goes zero, then we know it is convergent?

Here 
$$
\begin{align}
\frac{du}{dt}= Au + \text{I.C.} \\
\longrightarrow \mathbb{R}e  (\mathcal{X}(A))<0
\end{align}
$$
